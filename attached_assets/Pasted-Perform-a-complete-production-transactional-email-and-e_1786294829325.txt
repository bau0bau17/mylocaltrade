Perform a complete production transactional-email and email-verification audit for MyLocalTrade.

The immediate reported problem is that a real user requested a password-reset code but did not receive the email. Use the test address already provided in the attached screenshot to inspect the Brevo transactional logs, without unnecessarily repeating or exposing the full address in reports.

This is not limited to one inbox. Inspect and repair the shared email-delivery architecture so all existing application email flows work reliably.

REQUIRED SENDER FOR ALL APPLICATION EMAILS

All automated and transactional emails sent by MyLocalTrade must use:

From name:
MyLocalTrade

From email:
noreply@mylocaltrade.co.uk

This requirement applies to every application-generated email, including:

- Registration verification
- Verification-code resend
- Password reset
- Email-address verification/change
- Company Team invitations and invitation resend
- Account approval/rejection
- Quote and job notifications
- Hire, booking and appointment notifications
- Job completion notifications
- Review-related notifications
- Account deletion/security notifications
- Support/admin transactional notifications
- Any other automated email generated by the application

Do not leave individual routes or templates using old, inconsistent or personal sender addresses.

Create or reuse one central server-side sender configuration rather than hard-coding `noreply@mylocaltrade.co.uk` independently throughout multiple files.

Where environment configuration is appropriate, use a clear shared configuration such as:

EMAIL_FROM_NAME=MyLocalTrade
EMAIL_FROM_ADDRESS=noreply@mylocaltrade.co.uk

Do not expose these settings to the mobile client.

If an existing email legitimately needs replies, it may use an already-approved support address as Reply-To, while the visible From address must remain:

MyLocalTrade <noreply@mylocaltrade.co.uk>

Do not invent a new Reply-To address. Report the existing Reply-To configuration found.

BREVO SENDER AND DOMAIN VERIFICATION

Confirm in Brevo that:

- `noreply@mylocaltrade.co.uk` is authorised as a transactional sender;
- the `mylocaltrade.co.uk` sending domain is authenticated;
- SPF is configured correctly;
- DKIM is configured correctly;
- DMARC alignment is appropriate;
- Brevo does not report the sender/domain as unverified, suspended or restricted;
- production emails are not being sent from a Brevo default/test sender.

If DNS or Brevo owner action is required, do not guess or silently change unrelated DNS records. Report the exact required record/action and current status.

SECURITY

- Use BREVO_API_KEY_VERIFICATION only from Replit Secrets.
- Never expose the API key in code, logs, terminal output, screenshots, commits or reports.
- Never expose password-reset codes, verification codes, invitation tokens or magic links in logs.
- Keep all Brevo access server-side.
- Do not weaken generic password-reset responses or expose whether an account exists.
- Do not place real credentials in committed `.env` files.

IMMEDIATE PASSWORD-RESET INVESTIGATION

Trace the complete production path:

1. User requests password reset
2. API validates and rate-limits the request
3. Reset code is generated and securely stored
4. Email send is attempted using `MyLocalTrade <noreply@mylocaltrade.co.uk>`
5. Brevo accepts or rejects the request
6. Delivery, bounce, block or defer result
7. User enters the code
8. Code expiry, one-time use and password update

For the reported address, check the Brevo transactional logs and determine:

- whether MyLocalTrade attempted the send;
- which From address was used;
- provider message ID/status;
- delivered, deferred, soft bounce, hard bounce, blocked or spam status;
- whether the address is on a Brevo suppression/block list;
- whether sender/domain authentication or template configuration caused the failure.

Only unblock the specific address if the logs show it is safe and appropriate. Do not perform bulk suppression-list changes.

COMPLETE EMAIL INVENTORY

Search the complete codebase for every email sender, helper, template, route, background process and webhook.

Inspect every existing production email flow, including where applicable:

- Registration email verification
- Verification-code resend
- Password reset
- Email-address change verification
- Company Team invitations, resend and acceptance
- Trader/account approval or rejection
- Quote/job notifications
- Hire/booking/appointment notifications
- Completion and review notifications
- Account deletion/security notifications
- Support/contact/admin transactional emails
- Any other route, scheduled process or webhook that sends email

For every flow, confirm:

- it uses the shared sender configuration;
- From is `MyLocalTrade <noreply@mylocaltrade.co.uk>`;
- the correct production template/content is used;
- the recipient is correct;
- production links are correct;
- delivery failures are handled safely.

Report the exact inventory found. Do not invent new marketing-email features.

PRODUCTION CONFIGURATION

Verify:

- Production deployment has every required Brevo and sender environment variable.
- Development and production configurations are not accidentally mixed.
- `noreply@mylocaltrade.co.uk` is the production From address everywhere.
- Reply-To is valid only where replies are intentionally supported.
- Production templates and links do not point to development/preview URLs.
- Company Team invitation links use the correct production landing/app URL.
- Brevo sending status, account limitations and transactional-email availability are healthy.
- No email function silently falls back to console-only delivery in production.
- Provider failures are logged safely and can be diagnosed without leaking secrets.
- The mobile app never talks directly to Brevo.

EMAIL-VERIFICATION BEHAVIOUR

Confirm:

- Codes/tokens are cryptographically secure.
- Only hashes are stored where appropriate.
- Expiry is enforced.
- Successful use invalidates the code/token.
- Resend behaviour is safe and rate-limited.
- Old codes cannot be reused after resend or success.
- Repeated/concurrent requests do not produce inconsistent state.
- Error messages do not permit account enumeration.
- Email normalization is consistent.
- Verification state cannot be bypassed client-side.

DELIVERY AND USER EXPERIENCE

For each email type:

- Confirm the API genuinely attempts the send.
- Confirm Brevo accepts the request.
- Confirm the app displays an accurate generic success/failure state.
- Preserve anti-enumeration behaviour for password-reset and account-recovery flows.
- Do not tell a user an email was sent merely because of a console fallback.
- Provide safe internal diagnostics without exposing provider information to users.
- Check that subject, sender name and content look professional and consistently branded as MyLocalTrade.

TESTING

Use provider mocks for automated tests and a controlled real inbox for production delivery checks.

Test at minimum:

- Successful send from `MyLocalTrade <noreply@mylocaltrade.co.uk>`
- Unknown email
- Invalid email
- Provider authentication failure
- Unverified/invalid sender handling
- Provider timeout/error
- Rate limiting
- Expired code
- Reused code
- Resend and old-code invalidation
- Blocked/suppressed recipient
- Company invitation delivery
- Production URLs in verification/invitation links
- Correct Reply-To behaviour where applicable
- No secret/token/code leakage in logs or API responses
- Automated assertion that every transactional email uses the central From configuration

Do not send test emails to unrelated real users.

SCOPE

Do not change:

- Subscription products or RevenueCat
- Company Teams permissions/job logic
- Authentication rules unrelated to email verification
- Marketing-email functionality
- Database schema unless a confirmed security/correctness issue genuinely requires it
- DNS records unrelated to Brevo sender authentication

After implementation:

- run the complete relevant API tests and typechecks;
- perform a focused security and deliverability review;
- commit and push fixes to origin/main;
- do not deploy automatically unless explicitly authorised.

Report:

1. Root cause of the missing password-reset email
2. Brevo status for the reported attempt
3. Whether the address was blocked and what action was taken
4. Current Brevo verification status for `noreply@mylocaltrade.co.uk`
5. SPF, DKIM and DMARC status for `mylocaltrade.co.uk`
6. Complete email-flow inventory checked
7. Confirmation that every application email now uses `MyLocalTrade <noreply@mylocaltrade.co.uk>`
8. Reply-To configuration found/used
9. Configuration and code changes
10. Files changed and commit hash
11. Tests and controlled delivery checks
12. Whether backend republish is required
13. Whether a new mobile/TestFlight build is required
14. Any remaining Brevo, DNS or owner action required