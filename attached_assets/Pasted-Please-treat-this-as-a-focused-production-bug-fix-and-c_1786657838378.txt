Please treat this as a focused production bug fix and consistency audit, not a redesign or feature expansion.

The email system is displaying the wrong MyLocalTrade logo.

In the real Early Access confirmation email, the header currently shows an old/different icon: a house containing tools. That is not the approved MyLocalTrade logo.

The correct canonical logo is the attached image: the white crossed screwdriver and wrench on the blue rounded-square background. This exact logo is already used by the MyLocalTrade website and mobile app.

Please replace the incorrect email logo and ensure that this same existing canonical logo is displayed consistently in every email sent by MyLocalTrade, regardless of whether the email is triggered from the website, mobile app, Admin dashboard or backend.

Requirements:

Inspect the actual image URL generated by the shared email shell, including /api/public/logo.png, and identify exactly why it currently serves the old house-and-tools icon.

Locate and use the existing canonical logo asset already used by the website and mobile app. Do not redraw, regenerate, reinterpret or replace it with a similar icon.

Establish one canonical source for the MyLocalTrade logo so that the website, mobile app and email system cannot accidentally use different versions. Do not modify the appearance of the logo currently used on the website and app.

Ensure this exact logo is displayed in every email category, including:

account verification and OTP;

password reset and security alerts;

Early Access registration and confirmation;

trader and customer notifications;

leads, quotes, appointments and message notifications;

team invitations;

subscriptions and account lifecycle emails;

admin-triggered test emails;

Early Access launch and marketing campaigns;

Outreach campaigns;

every other email triggered by the website, app, Admin portal or backend.

Confirm that all 28 transactional email types and the Early Access/Outreach campaign renderers obtain the logo through the shared email shell. Find and remove or replace any old hardcoded email-logo references. Do not create duplicated email templates.

Serve the email logo from a stable, publicly accessible HTTPS URL on our own MyLocalTrade domain. Do not use the Google RCS storage URL or another third-party asset host.

The public logo endpoint must:

require no authentication;

return no redirect;

return the correct image/png MIME type;

serve the exact canonical logo;

have appropriate cache headers;

remain accessible to Gmail, Outlook, Apple Mail and Brevo.

Prevent email clients or CDNs from continuing to display the cached old icon. Introduce a new versioned URL, for example /api/public/mylocaltrade-logo-v2.png, or an equivalent stable versioned path.

Preserve the approved email designs completely. Do not change:

email colours;

layout or spacing, except where strictly required to fit the correct logo;

copy or subject lines;

CTA buttons;

destination links;

legal footers;

unsubscribe behaviour;

plain-text versions;

trader/customer/neutral template selection;

any email-sending logic.

Add regression tests proving that:

the shared email shell references only the canonical versioned logo URL;

every email category still uses the shared shell;

the public endpoint serves the expected canonical file;

its MIME type is correct;

no reference to the old house-and-tools asset remains;

if practical, the served file’s SHA-256 matches the selected canonical repository asset.

Generate real renderer previews in both Desktop and Mobile modes for at least:

Early Access confirmation;

OTP/security;

trader notification;

customer notification;

Early Access campaign;

Outreach campaign.

Run the complete API and Admin test suites, relevant typechecks and a focused code/security review. Fix any confirmed regression related to this change before committing.

After all automated checks and review pass, send only ONE real transactional test email to my controlled smoke-test address so I can visually confirm the logo in Gmail. Do not send a campaign, bulk email or outreach email. Keep MARKETING_BREVO_ENABLED=false.

Report:

the exact canonical source asset used;

its dimensions and SHA-256;

the new public HTTPS URL;

the cause of the old incorrect logo;

every affected file and email category;

preview and test results;

review findings and fixes;

the commit pushed to main;

whether a backend Publish is required.

This must remain a backend/email-template correction. Confirm explicitly that no new EAS/TestFlight mobile build is required.

Also inspect the sender identity separately. The small avatar shown by Gmail next to the sender name may be controlled by the Google account, Brevo sender settings, Gravatar or BIMI rather than by the HTML email template. Report its current source separately. Do not claim that replacing the logo inside the email will automatically change Gmail’s sender avatar.

SECURITY: The diagnostic screenshot contains a real Early Access confirmation URL/token. Treat it as sensitive. Verify that the token is already used, expired or invalidated. Never copy it into source code, tests, logs, previews, commits or the final report.

Do not change or regenerate the approved MyLocalTrade logo. The objective is to use exactly the same existing logo across the website, mobile app and every email sent by the platform.

Do not deploy, publish, enable marketing or start another EAS/TestFlight build. Stop after committing and pushing the verified correction, then tell me exactly what I need to Publish manually.